const settings = {
  log: true,
  logErrors: true,
  production: false,
  sqlConf: {
    user: `admin`,
    password: `BaX24w7sgyfxWr`,
    database: `biler`,
  },
  port: 3000,
};

module.exports = settings;
